{
  "name": "@iiiivaska/prism-vrt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Playwright over the gallery: visual regression for stories tagged vrt × scheme × density × viewport, and the web runtime contract in Chromium, WebKit and Firefox",
  "scripts": {
    "test": "playwright test",
    "test:update": "playwright test --update-snapshots=all",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
