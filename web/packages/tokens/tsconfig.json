{
  "extends": "../../../tsconfig.base.json",
  // Bundled by tsdown (ESM + .d.ts into dist/); `tsc` here only type-checks.
  "compilerOptions": {
    "module": "preserve",
    "moduleResolution": "bundler",
    "lib": ["es2024", "dom"],
    "jsx": "react-jsx",
    // The generated modules import each other with explicit `.ts` extensions, which the tools'
    // tests load under Node's type stripping as well.
    "allowImportingTsExtensions": true,
    "types": []
  },
  "include": ["src", "tsdown.config.ts"]
}
