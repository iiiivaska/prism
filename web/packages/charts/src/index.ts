/**
 * Implemented spec version per data-viz component and platform (ADR-0006 rule 2, ADR-0007, critic
 * G-21), the web twin of `DSChartsManifest` in swift/Sources/DSCharts/Manifest.swift. The parity
 * report (roadmap P2-3) compares it with `specVersion` in the chart specs. Edited by hand in the
 * same change as the component, in ./manifest.ts.
 */
export { implemented, type ImplementedVersions, type WebPlatform } from "./manifest.ts";
