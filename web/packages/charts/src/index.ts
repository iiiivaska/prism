/**
 * Implemented spec version per data-viz component (ADR-0007), the web twin of `DSChartsManifest`
 * in swift/Sources/DSCharts/Manifest.swift. The parity report (roadmap P2-3) compares it with
 * `specVersion` in the chart specs; the first charts arrive with data-viz wave 1.
 */
export const implemented: Readonly<Record<string, number>> = {};
