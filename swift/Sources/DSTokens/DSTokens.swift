// The one hand-written file of this target: tools/tokens generates the rest into Generated/ and Resources/
// (ADR-0004, tools/tokens/ARCHITECTURE.md §9.7). Do not add token values here.
public enum DSTokensInfo {
    /// System version, stamped from VERSION by the release workflow.
    public static let version = "0.1.0"
}
