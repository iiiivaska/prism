/// Implemented spec versions per component (ADR-0006). The parity report compares this map
/// with `specVersion` in spec/components/*.yaml. Edited by hand in the same change as the component.
public enum DSComponentsManifest {
    public static let implemented: [String: Int] = [:]
}
