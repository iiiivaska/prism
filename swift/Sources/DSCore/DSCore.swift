import DSTokens

/// Theme root, environment values, accessibility policy, surface resolution, font registration,
/// haptics and motion live here (ADR-0003, ADR-0009, ADR-0011). Implemented by the vertical-slice tickets.
public enum DSCoreInfo {
    public static let version = DSTokensInfo.version
}
