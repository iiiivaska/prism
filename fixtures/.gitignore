# Generated by tools/release/fixture-apple.ts: the package reference is what differs between a
# rehearsal and a real consumer, so the project is written per run and the sources are committed.
swiftui-app/*.xcodeproj/
# A stray local build; the fixtures are built in a work directory, never in place.
*/node_modules/
*/dist/
*/*.tgz
