// Generated by tools/tokens from tokens/prism.resolver.json. Do not edit; run `pnpm tokens:build`.
import SwiftUI

/// Every token value for one context (ARCHITECTURE §9.7.4): the brand's catalog colors as `color` (ADR-0020 §7),
/// everything else from per-axis switch tables. DSCore caches one per context.
public struct DSTokenSet: Hashable, Sendable {
    public let context: DSTokenContext
    /// The colors of the context's brand, from its catalog namespace.
    public let color: DSColor
    public let material: Material
    public let border: Border
    public let elevation: Elevation
    public let space: Space
    public let size: Size
    public let radius: Radius
    public let typography: Typography
    public let motion: Motion
    public let gradient: Gradient
    public let interaction: Interaction
    public let components: Components

    public init(_ context: DSTokenContext = .default) {
        self.context = context
        let color = DSColor(brand: context.brand, transparency: context.transparency)
        let material = Material(context)
        let border = Border(context)
        let elevation = Elevation(context)
        let space = Space(context)
        let size = Size(context)
        let radius = Radius(context)
        let typography = Typography(context)
        let motion = Motion(context)
        let gradient = Gradient(context)
        let interaction = Interaction(context)
        self.color = color
        self.material = material
        self.border = border
        self.elevation = elevation
        self.space = space
        self.size = size
        self.radius = radius
        self.typography = typography
        self.motion = motion
        self.gradient = gradient
        self.interaction = interaction
        self.components = Components(context, color: color, material: material, elevation: elevation, radius: radius, typography: typography, motion: motion)
    }
}
