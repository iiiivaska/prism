{
  "description": "A ref set source writes ref.space.2 next to its \"$ref\"; DTCG Resolver 2025.10 §4.2.2 makes such keys overrides, which Prism rejects instead of ignoring.",
  "expect": [
    {
      "code": "resolver/unsupported-source"
    }
  ]
}
