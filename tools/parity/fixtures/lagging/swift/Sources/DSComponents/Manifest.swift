public enum DSComponentsManifest {
    public static let implemented: [String: [String: Int]] = [
        // Button is specVersion 7 in the repository: iOS is one version behind and nothing else is built.
        "Button": ["ios": 6],
    ]
}
