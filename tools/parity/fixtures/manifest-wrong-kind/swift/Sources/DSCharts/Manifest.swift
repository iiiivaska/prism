public enum DSChartsManifest {
    public static let implemented: [String: [String: Int]] = [
        // Button is a primitive: it belongs to DSComponentsManifest (ADR-0012).
        "Button": ["ios": 3],
    ]
}
