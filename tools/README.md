# tools

Build and check tooling for Prism, in TypeScript, invoked through the `@iiiivaska/prism-tools` scripts in `package.json` (from the repository root: `pnpm --filter @iiiivaska/prism-tools <script>`). Each planned folder is a ticket in `docs/roadmap.md`.

## How tools run

- **From source on Node 24.** `node lint/literals.ts` runs the TypeScript file directly through Node's native type stripping: no build step, no loader, no `--experimental-strip-types` flag. The workspace pins Node 24 in `.node-version`.
- **Only syntax Node can strip.** `tsconfig.json` enforces it: `erasableSyntaxOnly` (no `enum`, `namespace` or parameter properties), `verbatimModuleSyntax` (type-only imports use `import type`), `allowImportingTsExtensions` (relative imports end in `.ts`), `module: nodenext`, `noEmit`. `pnpm typecheck` runs `tsc` over the package, which only checks types.
- **Tests** use Vitest 4 (`vitest.config.ts`): `**/*.test.ts` next to the code they test, with fixture trees under `<tool>/fixtures/`, which tsc and ESLint skip. `pnpm test` runs them.
- **No tsdown config (P0-2 decision).** The roadmap listed "TypeScript + tsdown config" for this package. Tools are private, never published and always run from source, so a bundle would be a second, possibly stale copy of the same code. Type safety comes from `tsc`, not from a bundler. Add tsdown only if a tool has to ship outside the repository (for example as a CLI for consuming apps); the published web packages under `web/packages/` do build with tsdown.
- **Dependencies** come from the workspace catalog (`catalog:` in `package.json`, versions in `pnpm-workspace.yaml`). The Phase 1 tools use Style Dictionary (one run per layer stack in `tokens/engine/`), `@terrazzo/cli` and `@terrazzo/parser` (`tokens:lint`, the oracle test), Color.js (color math and gamut mapping), Ajv with `ajv-formats` (`tokens:validate`, which the Figma flavor test reuses, and the `app.prism` and `brand.json` schemas of the source checks), `jsonc-parser` (the source model, `tokens:normalize` and the pairs file of `contrast:check`), Motion (the spring generator of `tokens/transforms/spring.ts`), `opentype.js` (`fonts:check`), `woff2-encoder` (the web font files), `yaml` (changeset front matter in `tokens:diff`, and `tokens/docs.test.ts`), `tailwindcss` with `@tailwindcss/node` in tests only (the `tailwind.css` compile test), and `@tokens-studio/types` for types only (the Tokens Studio flavor).

## What exists now

| Script | File | What it does | CI |
|--------|------|--------------|----|
| `typecheck` | `tsconfig.json` | `tsc` over every tool and test | `web` job, via root `pnpm typecheck` |
| `test` | `vitest.config.ts` | Vitest over `**/*.test.ts` | `web` job, via `pnpm -r test` |
| `lint:literals` | `lint/literals.ts` | Literal-values guard (`tokens/README.md`, "Rules" 1): scans `swift/Sources` and `web/packages/*/src` (generated output under `swift/Sources/DSTokens/Generated` and `src/generated` excluded), prints `path:line:column` for each hit and exits 1. One pattern table serves every kind, so no name is reported twice: `color` (hex and numeric colors such as `#0a84ff`, `rgb(0 0 0)`, `Color(red:…)`), `dimension` (`px`/`pt`), `font` (family names), `motion` (ADR-0023 §12, P1-4: literal durations, easings and springs, bare `withAnimation`, `generateLinearEasing`, and `settlingDuration`, the one rule that also scans `swift/Tests`), `typography` (ADR-0021 §12, P1-4) and `runtime` (ADR-0019 rule 1, P1-5: the `data-ds-*` attributes, the preference and pointer media features and Tailwind's media-only variants in TS, JS and CSS, `*.test.*` files excluded). It skips comments and scans string contents. `--root <dir>` scans another tree with the same layout; `lint/fixtures/{motion,runtime,typography}/{hit,miss}/` hold one hit and one miss per pattern of those kinds. The kinds `material` (ADR-0022 rule 2, P3-1/P3-4) and `brand` (ADR-0020 rule 13, P3-1/P3-2) join the same table. | `contracts` job |
| `tokens:validate` | `tokens/validate.ts`, `tokens/schema/dtcg-2025.10/` | Offline DTCG 2025.10 schema validation (P1-1, ADR-0024 §10) of `tokens/**/*.tokens.json`, `brands/*/brand.tokens.json` and the resolver against vendored schemas checked by SHA-256, one Ajv 8 instance with `ajv-formats` per schema; prints `file#pointer` per error and exits 1 (2 on a usage or layout error). See "DTCG schema validation" below. | `contracts` job, ungated |
| `tokens:lint` | `tokens/terrazzo.config.ts`, `tokens/lint-orthogonality.ts` | P1-2, ADR-0024 §9: `tz lint` (DTCG strictness, kebab-case names as errors), then `tokens/lint-orthogonality.ts`, which requires `@terrazzo/parser`'s `resolver.orthogonal` and a permutation count equal to the product of the context counts (432), and names every id two modifiers write. Exit 1 on a failure, 2 on a usage or load error. `--root <dir>` checks `<dir>/tokens/prism.resolver.json` instead, for example `pnpm tokens:lint --root tokens/fixtures/non-orthogonal`, which exits 1 naming `sys.interaction.hover`. `tokens:build` repeats the check authoritatively. | `contracts` job (gate `tokens/terrazzo.config.ts`) |
| `lint:reference-copy` | `lint/reference-copy.ts`, `lint/reference-copy.denylist.txt` | Reference-copy guard (ADR-0015 rules 1–3, critic R-01): scans `spec/`, `tokens/`, `brands/`, `agent/`, `web/packages/*/src`, `web/apps/*`, `swift/Sources`, `swift/Tests`, `gallery/` and `docs/research/fonts/` for the strings in the denylist, which is derived from the `docs/research/refs-*.md` analyses; prints `path:line` for each hit and exits 1 (2 on a usage, denylist or layout error). Matching is case-insensitive and ignores spacing and line breaks; comments count. HTML and SVG are also read as visible text (tags removed, entities decoded), Markdown also without emphasis and code delimiters, and every file also with its backslash escapes decoded (`\u0412`, `\'`). Exempt: the analyses that quote the copy on purpose (`refs-*.md`, `visual-dna.md`, `critic.md`) and the denylist. Skipped: `node_modules`, `.git` and build output (`dist`, `.turbo`, `storybook-static`, `test-results`, `playwright-report`, `.build`, `DerivedData`). `--root <dir>` scans another tree with the same layout and `--denylist <file>` reads another list; the tests use an invented one, so no reference copy is committed as test data. | `contracts` job |
| `tokens:normalize`, `tokens:normalize:check` | `tokens/normalize.ts` | Source hygiene (P1-4, ARCHITECTURE §7.13): rule `hex` (the roadmap's `normalize-hex`: every authored `hex` equals the hex of the CSS-gamut-mapped sRGB color) and rule `spring-fallback` (a spring token's `$value.duration` and `app.prism.spring.settle` equal the derived settle time and its `$value.delay` is 0 ms, ADR-0023 rule 3). `tokens:normalize` rewrites the stale values in place with surgical `jsonc-parser` edits that keep every other byte; `tokens:normalize:check` prints `file:line  pointer  expected → actual` and exits 1. `pnpm tokens:normalize --check`, the form ADR-0023 rule 3 names, reaches the tool as `--write --check`, so `--check` wins and nothing is written. `--root <dir>` checks another tree (`tokens/fixtures/tampered-hex`, `tokens/fixtures/tampered-spring`). | `contracts` job (gate `tokens/normalize.ts`); `tokens:build` runs the check first |
| `tokens:build`, `tokens:check` | `tokens/build.ts` and the modules of `tokens/README.md` | The token build (P1-3 to P1-5, P1-8; ADR-0004): the normalizer check, the resolver driver (432 permutations, one Style Dictionary 5.5.3 run per distinct layer stack), the source checks and IR analysis, every format (CSS with modes, `motion.css`, Tailwind `@theme inline`, TS maps, `runtime.ts`, `manifest.json`, Swift sources, `Colors.xcassets`, the font files, the Tokens Studio and Figma-native flavors), the cascade simulator and table checks of `tokens/verify/`, then the writer, which owns the roots of `tokens/config.ts` `OWNED_ROOTS`. Nothing is written while any step reports a diagnostic. `tokens:check` compares with disk, writes nothing and exits 1 on any difference. Flags and outputs: `tokens/README.md`, "The build (P1-5)". | `contracts` job (gate `tokens/build.ts`): `tokens:build`, then `git status --porcelain` over the owned roots, `swift/Sources/DSTokens/Resources/Fonts` included (gate `tokens/formats/fonts.ts`) |
| `bash tokens/scripts/verify-xcassets.sh`, `xcodebuild test` of `DSTokensTests` | `tokens/scripts/verify-xcassets.sh`, `../swift/Tests/DSTokensTests/ColorCatalogTests.swift`, `../.swiftpm/xcode/xcshareddata/xcschemes/DSTokensTests.xcscheme` | The color catalog where it compiles (P1-5, ARCHITECTURE §9.7.3, §9.8): the script compiles `Colors.xcassets` with actool for iphoneos, macosx and watchos (warnings are errors) and checks `<namespace>/color-text-secondary` in each `Assets.car` with `assetutil`; `ColorCatalogTests` compare every colorset of every brand namespace with `DSColorToken.appearances` on the iOS Simulator (scheme `Prism-Package`) and the watchOS Simulator (the shared scheme `DSTokensTests`, because `Prism-Package` also builds DSSnapshotTests, whose SnapshotTesting does not compile for watchOS; ARCHITECTURE V1). Host `swift test` skips the suite: it copies the catalog uncompiled. | `apple` job (gates `tokens/scripts/verify-xcassets.sh`, `ColorCatalogTests.swift` and, for watchOS, the scheme, each with `tokens/build.ts`) |
| `contrast:check` | `contrast/check.ts`, `contrast/{pairs,gradient,thresholds,report}.ts` | P1-6 (ADR-0011, ADR-0020 rule 10, ADR-0022): evaluates every pair of `tokens/contrast-pairs.json` for every brand in all six colorScheme contexts (platform web, other axes at their defaults; `api.contrastContexts` refuses a color that varies along another axis or between web and Apple), compositing translucent colors source-over in gamma sRGB over `underlays` and `backdrops`, gradient globs included; `stops: "all"` is ADR-0022 V1 (every stop and 100 samples per segment in gamma sRGB and OKLab) and `region: "card-header"` is V2 (twelve Card geometries). It also checks that every `a11y.pairsWith` of a `sys.color.text.*` token is a pair and that V1 and V2 reach every vivid gradient. Prints a Markdown table (also to `$GITHUB_STEP_SUMMARY`) and exits 1 on any failure. `--root <dir>`, `--resolver <path>`, `--pairs <path>`, `--report <file>`; `contrast/fixtures/` holds a broken pair and gradients that break V1 and V2. | `contracts` job (gate `contrast/check.ts`) |
| `tokens:diff` | `tokens/diff.ts`, `tokens/diff/{git,classify,changesets,report}.ts` | P1-7 (ARCHITECTURE §11, ADR-0024 §14): builds the last release tag's sources (read from git by this checkout's tool code) and the working tree, classifies every token change (removal, type change or axis change = major; addition, value change, deprecation = minor; one level lower while the base tag is 0.x) and fails when the pending changesets in `.changeset/` declare a lower bump for the token package than the changes require (a package fixed with it counts, ignored packages never do). No release tag yet: it passes with a notice. `--base <ref>`, `--changesets <dir>`, `--json`, `--allow-invalid-baseline <reason>` (people only), `--root`, `--resolver`. | `contracts` job (gate `tokens/diff.ts`), pull requests only |
| `fonts:check` | `fonts/check.ts`, `fonts/font-file.ts`, `fonts/brands.ts` | P1-8 (ADR-0021 §11, ADR-0020 rule 14): every font file a repo brand serves, read with `opentype.js`, against its `brand.json` entry: the file and an `OFL.txt` beside it, SHA-256, version (name ID 5), Russian Cyrillic incl. Ё/ё, tabular digits (equal 0–9 advances with or without GSUB `tnum`), PostScript instance names, plus the 2 MiB budget of the files bundled into DSTokens. One table row per file and check; exit 1 on any failure. `--root <dir>`, `--json`. The font files themselves are emitted by `tokens:build` (`tokens/formats/fonts.ts`, woff2 through `woff2-encoder`). | `contracts` job (gate `fonts/check.ts`) |

Limits of the literal guard: it is a lexer plus regular expressions, not a parser. It cannot see unit-less numbers such as SwiftUI's `.padding(12)`, which review and the component specs still have to catch. Strings are assumed to be single-line, except for template literals.

Limits of the reference-copy guard: it finds listed strings, not paraphrased or translated copy or a recognizable composition; ADR-0015's reference-distance review still covers those. Binary files are skipped, so text baked into a PNG is out of reach: re-render images from cleaned sources. Strings assembled at run time, or encoded other than by backslash escapes (percent-encoding, base64), pass unseen. Files outside the scanned folders, such as `tools/`, the rest of `docs/`, root files and `web/packages/*` outside `src`, are not checked. Entries are added by hand: a new reference or analysis needs new entries.

## Planned tools

CI calls each script only once its file exists: the `plan` job in `.github/workflows/ci.yml` tests for the file and opens the matching gated step. No workflow edit is needed when a tool lands.

Gate files are relative to `tools/`. The root `package.json` passes every Phase 1 script through (`pnpm tokens:lint` from the repository root runs the one here); arguments after the script name reach the tool. Every Phase 1 tool has landed (above); the Phase 2 tools remain.

| Folder | Purpose | Script | Ticket | Gate file | CI |
|--------|---------|--------|--------|-----------|----|
| `spec/` | JSON Schema validation of `spec/components/*.yaml` and `spec/patterns/*.yaml`; checks that token paths exist in the built dictionary, that bindings are spec-bindable `sys` roles or the spec's own `comp` tokens, that every `comp` token is bound and that prose paths resolve (`spec/SCHEMA.md`, ADR-0024 §5) | `spec:validate` | P2-1 | `spec/validate.ts` | `contracts` |
| `icons/` | Registry schema validation, Phosphor catalog cross-check, codegen for the `DSIconName` enum (Swift; the `DSIcon` view keeps its name) / `iconRegistry` (TypeScript) (`spec/icons/README.md`) | `icons:validate` | P2-2 | `icons/validate.ts` | `contracts` |
| `icons-apple/` | Swift package: SF Symbols availability at the minimum OS (macOS job) | `swift run` in the `apple` job | P2-2 | `icons-apple/Package.swift` | `apple` |
| `parity/` | Reads the specs and both manifests, writes `parity/report.md`, fails on lag for `full`/`adapted` platforms (ADR-0006) | `parity:report` | P2-3 | `parity/report.ts` | `contracts` |
| `licenses/` | Validates `licenses/inventory.json`, fails when a packaged item lacks a permitted license, regenerates `THIRD_PARTY_NOTICES.md` (ADR-0015) | `licenses:check`, `licenses:notices` | P2-4 | `licenses/check.ts` | `contracts` (the check) |

## DTCG schema validation

CI validates every token file, brand file and the resolver against the DTCG 2025.10 schemas with `pnpm --filter @iiiivaska/prism-tools tokens:validate` (`pnpm tokens:validate` from the root), an ungated `contracts` step (ADR-0024 §10). It runs offline, with Ajv 8 and `ajv-formats` from the lockfile and one Ajv instance per schema. The schemas are vendored in `tools/tokens/schema/dtcg-2025.10/` with a `SHA256SUMS` file and the W3C license text (inventory item `dtcg-schemas-2025-10`), and the script checks the bytes before it validates. `--root <dir>` validates another tree with the same layout. To refresh the schemas, download `https://www.designtokens.org/schemas/2025.10/{format,resolver}.json`, update `SHA256SUMS` and review the diff in its own change.
