---
"@iiiivaska/prism-tokens": minor
---

Direction-board sign-off (P1-9, ADR-0029 and ADR-0030): glass follows the color scheme with the `text.on-glass-fill*` tones, a brighter light vivid set with one temperature per 2×2 pair and the new `night-lagoon` gradient, a `watch` density, and new semantic roles for maps, charts on media and glass, inverse on media, borders on media, secondary text on accent, `type.axis` and the edge and highlight colors.

Removed: `comp.button.ghost.bg.rest` (the ghost button has no rest fill), `ref.blur.bloom`, `ref.color.status.*.tint-light`; `sys.type.metric.xl` moves into the platform files (40 px on watchOS).
